# Provenance

The workspace inspection contract and its regression scenarios were adapted
from the user-supplied Bounded Engineering 0.1 integration input. That input
states that it did not copy third-party skill code. The implementation remains
under this repository's MIT license and relies only on Python's standard library
and the installed Git command.

Public behavior references: the Git worktree porcelain contract and the Agent
Skills specification. No private business source, conversation transcript,
credential, or host-specific path is included.
