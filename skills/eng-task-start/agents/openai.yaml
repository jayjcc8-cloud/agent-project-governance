interface:
  display_name: "Task Knowledge Retrieval"
  short_description: "Retrieve relevant knowledge once at task start"
  default_prompt: "Use $eng-task-start to search once for knowledge relevant to this real task; continue if none exists."
policy:
  allow_implicit_invocation: false
