interface:
  display_name: "Exceptional Context Recovery"
  short_description: "Recover only interrupted task context"
  default_prompt: "Use $context-governance only for this real interruption or handoff; reuse existing recovery state if needed."
