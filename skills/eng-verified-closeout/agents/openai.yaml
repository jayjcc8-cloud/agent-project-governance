interface:
  display_name: "Conditional Task Learning"
  short_description: "Learn only after acceptance when needed"
  default_prompt: "Use $eng-verified-closeout after task acceptance; skip writing unless knowledge is new or needs updating."
policy:
  allow_implicit_invocation: false
